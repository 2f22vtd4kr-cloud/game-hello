Отлично, давай обновим бота с учетом реальной специфики Севастополя и интерактивной карты fuel.sevtech.org/map. 

Выполни следующие задачи для перестройки архитектуры:

1. Настройка Гео-сценария с выбором районов Севастополя:
- После того как пользователь выбрал тип топлива (например, АИ-95), бот должен отправить сообщение:
  "📍 Система квотирования топлива в Севастополе. Для поиска работающих станций ТЭС и проверки суточных лимитов поделитесь геолокацией или выберите район города вручную:"
- Кнопки выбора: `KeyboardButton("📍 Поделиться геолокацией", request_location=True)`, а также кнопки ручного выбора (отказа от гео): "Гагаринский район", "Ленинский/Нахимовский", "Балаклава/Северная".

2. Интеграция данных карты fuel.sevtech.org/map в меню АЗС:
- Когда пользователь переходит к списку доступных заправок (например, ТЭС), обязательно добавляй в текст сообщения следующее предупреждение:
  "⚠️ Внимание! Перед выездом на АЗС обязательно сверяйте фактическое наличие горючего на официальной интерактивной карте: https://fuel.sevtech.org/map"
- Добавь под список заправок inline-кнопку "🗺️ Открыть карту остатков АЗС", которая ведет на https://fuel.sevtech.org/map.

3. Логика выдачи одноразовых QR-кодов из Базы Данных (SQLite):
- Создай таблицу `vouchers` (id, fuel_type, station_brand, district, qr_code_payload, status). При первом запуске заполни её тестовыми кодами (статус "доступен") для разных районов города.
- Когда пользователь нажимает кнопку "🤖 Тестовая оплата (Симуляция)", бот берет из базы один свободный ваучер, меняет его статус на "выдан" и с помощью библиотеки `qrcode` генерирует QR-код.
- Бот отправляет QR-код пользователю с текстом: 
  "🎉 Ваш персональный QR-код на 20 литров успешно сгенерирован! 
  
  📋 Инструкция:
  1. Код действует до момента гашения сотрудником АЗС.
  2. На заправке ТЭС предъявите код контролёру Правительства Севастополя.
  3. Контролёр сверит данные кода с госномером вашего авто и погасит его, после чего вы сможете заправиться.
  4. Повторный код для этой машины можно получить только через 7 дней."

Обнови код бота, примени изменения в базе данных и перезапусти проект.
