Выполни следующие задачи:

1. Интеграция Crypto Pay API для создания счетов (Invoices):
- Замени прошлую симуляцию оплаты в блоке "⚡ Заказать дополнительный объем (Платная доза)" на генерацию реального инвойса через Crypto Bot.
- URL для создания счета: https://pay.cryptoboot.org/api/createInvoice (или используй официальную библиотеку aiocryptopay, если так удобнее).
- При создании инвойса передавай параметры:
  * `asset`: 'USDT' (валюта, в которой вы получите средства)
  * `amount`: эквивалент стоимости 20 литров топлива в USDT (например, 12-15 USDT, укажи фиксированную сумму в коде)
  * `payload`: уникальный ID заказа из нашей БД SQLite (чтобы связать оплату с пользователем)
  * `description`: "Оплата дополнительного ваучера на 20 литров"
- Полученную от API ссылку (`pay_url`) привяжи к инлайн-кнопке: "💳 Оплатить через CryptoBot (СБП/Карты/Крипта)".

2. Подключение Веб-сервера для Webhooks (Моментальная выдача):
- Наш эндпоинт веб-сервера (FastAPI / aiohttp) теперь должен слушать уведомления по адресу `/webhook/cryptobot`.
- При поступлении POST-запроса от CryptoBot об успешной оплате счета (статус "paid"): бот извлекает ID заказа из `payload`, находит запись в SQLite, меняет статус ваучера на "выдан", генерирует QR-код с жестко вшитым госномером машины и отправляет его пользователю в чат Telegram.
- Обязательно добавь проверку подлинности вебхука с помощью хеширования SHA256 и секретного токена, как указано в документации Crypto Pay.

3. Параметры авторизации из Secrets:
- Токен авторизации должен строго подтягиваться из переменной окружения (Secrets):
  CRYPTO_BOT_TOKEN

4. Обновление строки статистики:
- В описание админки (/stats) добавь строку: "📈 Текущий статус вывода: Прямой прием оплат в USDT через Crypto Pay API без задержек".

5. Сохранение всей архитектуры:
- Полностью сохрани всю прошлую логику: синее меню команд, строгую валидацию госномеров РФ, правила и сводки Губернатора, лимит 1 бесплатный код в неделю, лимит 1 платная доза в неделю и админ-панель /admin с интерактивной кнопкой обновления статистики.

Обнови код приложения и перезапусти бота.
